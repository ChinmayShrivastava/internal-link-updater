from main.type import LinksToUpdate, LinkToUpdate

# modify these links as necessary for each iteration
LINKS = LinksToUpdate(
    links=[
        LinkToUpdate(
            old_link="https://www.example.com",
            new_link="https://www.example.com"
        ),
        LinkToUpdate(
            old_link="https://www.example.com",
            new_link="https://www.example.com"
        )
    ]
)
